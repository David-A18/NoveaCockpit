# NoveaCockpit Sample GUI

This is a static fake-data preview of the NoveaCockpit product shape.

It does not connect to local APIs, import transcripts, call models, expose prompts, or include private app code.

Use `images/` only for screenshots that have been reviewed and approved for public sharing.
